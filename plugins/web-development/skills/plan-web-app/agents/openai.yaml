interface:
  display_name: "Plan Web App"
  short_description: "Plan web apps and optional GitHub tracking"
  default_prompt: "Use $plan-web-app to plan or align this web app’s living guidance and optional GitHub work tracking."
policy:
  allow_implicit_invocation: true
